name: Publish NuGet

on:
  push:
    branches: [release]
    paths:
      - 'src/**'
      - 'eng/Versions.props'
      - 'Directory.Build.props'
      - 'Directory.Packages.props'
      - 'README.md'
      - 'LICENSE'
      - '128x128_compressed.png'
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest
    environment: release
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-dotnet@v5
        with:
          global-json-file: global.json
      - name: Restore and test
        run: |
          dotnet restore Snapshot.sln
          dotnet test Snapshot.sln -c Release --no-restore --filter "FullyQualifiedName!~SnapshotEndToEndTests"
      - name: Pack libraries and platform CLI packages
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force artifacts/packages | Out-Null
          dotnet pack src/Snapshot.Protocol/Snapshot.Protocol.csproj -c Release --no-restore -o artifacts/packages
          dotnet pack src/Snapshot.Playwright/Snapshot.Playwright.csproj -c Release --no-restore -o artifacts/packages
          dotnet pack src/Snapshot.Cli/Snapshot.Cli.csproj -c Release -o artifacts/packages
          foreach ($rid in @('win-x64', 'linux-x64', 'linux-arm64', 'osx-x64', 'osx-arm64')) {
            dotnet pack src/Snapshot.Cli/Snapshot.Cli.csproj -c Release -r $rid -p:PublishAot=false -o artifacts/packages
          }
      - name: Validate packed CLI installation
        shell: pwsh
        run: |
          $version = (Select-String -Path eng/Versions.props -Pattern '<SnapshotPackageVersion>(.*?)</SnapshotPackageVersion>').Matches.Groups[1].Value
          dotnet tool install Snapshot.Cli --version $version --tool-path artifacts/tool-test --add-source artifacts/packages --ignore-failed-sources
          ./artifacts/tool-test/snapshot help
      - name: NuGet login using trusted publishing
        uses: NuGet/login@v1
        id: login
        with:
          user: ${{ secrets.NUGET_USER }}
      - name: Publish packages
        shell: bash
        run: |
          set -euo pipefail
          version="$(sed -n 's:.*<SnapshotPackageVersion>\(.*\)</SnapshotPackageVersion>.*:\1:p' eng/Versions.props)"
          cli_pointer="artifacts/packages/Snapshot.Cli.${version}.nupkg"

          # Publish dependencies and RID-specific CLI packages before the top-level
          # pointer package so dotnet tool install can always resolve its platform.
          for package in artifacts/packages/*.nupkg; do
            if [[ "$package" == "$cli_pointer" ]]; then
              continue
            fi
            dotnet nuget push "$package" \
              --api-key "${{ steps.login.outputs.NUGET_API_KEY }}" \
              --source https://api.nuget.org/v3/index.json \
              --skip-duplicate
          done

          if [[ -f "$cli_pointer" ]]; then
            dotnet nuget push "$cli_pointer" \
              --api-key "${{ steps.login.outputs.NUGET_API_KEY }}" \
              --source https://api.nuget.org/v3/index.json \
              --skip-duplicate
          fi
